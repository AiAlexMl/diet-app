# מפת דרכים — DietAI

> מסמך תכנון לשלבים הבאים. מתעדכן עם ההחלטות. נכתב: יוני 2026.

## החזון העסקי

1. **חברות מזון** משלמות כדי שהמוצרים שלהן (עם תמונות שהן מספקות) יופיעו בתפריטים.
2. **מאמני כושר** מקבלים גרסה ממותגת (לוגו/צבעים שלהם) ומפיצים אותה למתאמנים — מנוע צמיחה.
3. **משתמשים** צוברים נקודות ופותחים פיצ'רים (תפריט אלטרנטיבי עם קפה / שתיה קלה וכו').

**עקרון מנחה**: יציבות ואמינות לאורך זמן; שירותים חינמיים עד שכמות המשתמשים תצדיק תשלום.

## המודל העסקי (הוחלט: יוני 2026)

- **המאמנים הם הלקוח המשלם.** Freemium: חינם בהתחלה (הם מנוע השיווק) → מנוי ₪39–99/חודש לתכונות פרו (צפייה בתפריטי מתאמנים, ניהול, דומיין ממותג). מאמנים כבר משלמים היום על כלים — הכסף שלהם זמין עכשיו.
- **משתמשי קצה — חינם לתמיד.** גבייה ממשתמשים הורגת את הצמיחה.
- **חברות מזון — מקור הכנסה שני, לא הבסיס.** פרסום דורש עשרות אלפי משתמשים פעילים; נכנס רק בקנה מידה, עם נתוני events כהוכחה.

## ⚠️ סיכון משפטי — לטפל לפני גביית כסף

בניית תפריטי תזונה היא עיסוק מוסדר בישראל (תזונאים/דיאטנים). אפליקציה חינמית עם דיסקליימר ≠ מאמנים שמפיצים "תפריטים" בכסף תחת המותג שלהם. צעדים: למסגר כ"תפריט לדוגמה/השראה", דיסקליימר מחוזק בגרסת המאמנים, **ייעוץ משפטי קצר לפני שקל ראשון**.

## תלות בין הרעיונות (למה הסדר הזה)

```
תפריט עם פינוק (קפה/שתיה)  ←  אין תלות, אלגוריתם בלבד — אפשר עכשיו
נקודות                      ←  דורש חשבונות משתמשים (שלב 1)
גרסת מאמנים                 ←  דורש שרת לאחסון מיתוג (שלב 1)
חברות מזון                  ←  דורש הוכחת כמות משתמשים = אנליטיקס (שלב 1) + משתמשים רבים (שלב 2)
```

---

## שלב 0 — Quick win: תפריט אלטרנטיבי עם פינוק (ללא שרת)

כפתור במסך התפריט: "אני רוצה גם ☕ / 🥤". המנגנון:
- מאגר "פינוקים" קטן ב־data.js (קפה הפוך ~60 קק"ל, 2 כוסות קולה זירו ~0, קולה רגילה ~140, חטיף וכו').
- `buildMenu()` עם פרמטר `treat`: מקצים את קלוריות הפינוק מראש (מורידים מה־target לפני הבנייה), הפינוק מוצג כשורה בתפריט. `reconcile` כבר ידאג שהכל יסתדר.
- בהמשך (שלב 3) הכפתור הזה יינעל מאחורי נקודות — אבל הלוגיקה כבר תהיה בנויה.

**מאמץ**: קטן. **ערך**: פיצ'ר נראה לעין שמבדל את האפליקציה.

## שלב 0.5 — דף נחיתה למאמנים (ולידציה לפני בנייה)

עמוד סטטי `coaches.html` (אותו עיצוב, ללא שרת): הצעת הערך למאמן ("האפליקציה שלך, המותג שלך, המתאמנים שלך") + טופס הרשמה מוקדמת (Google Form בהתחלה; Supabase כשיהיה).
- **המטרה האמיתית: ולידציה.** לפני שבונים דשבורד מאמנים — בודקים ביקוש. 20 נרשמים → בונים את שלב 2; 2 נרשמים → חושבים מחדש. למכור לפני שבונים.
- בהמשך אותו עמוד ישמש גם כ‑media kit לחברות מזון (מספרי שימוש מה‑events).
- **מאמץ**: קטן מאוד. אפשר לפני או במקביל לשלב 1.

## שלב 1 — תשתית: Supabase (הבסיס להכל)

- **חיבור**: ה־SDK של Supabase נטען בתג `<script>` מ־CDN — מתאים בדיוק לארכיטקטורה הקיימת (ללא build). GitHub Pages נשאר האחסון; אין צורך בשרת משלנו.
- **Auth**: התחברות עם Google / קישור-קסם למייל. **המצב האנונימי נשאר** — מי שלא נרשם ממשיך להשתמש כרגיל (חשוב לא להציב חומת-הרשמה לפני שיש ערך).
- **טבלאות ראשונות**:
  - `profiles` — פרטי המשתמש (גיל/משקל/יעד/העדפות) ⇒ לא למלא מחדש בכל כניסה.
  - `menus` — תפריטים שנשמרו (היסטוריה, "התפריט שעבד לי").
  - `events` — אנליטיקס פנימי: כל יצירת תפריט נרשמת (אנונימית). **זה הנתון שמוכרים איתו לחברות מזון** ("X תפריטים נוצרו החודש").
- **אבטחה**: מפתח ה־anon של Supabase חשוף בקוד (זה בסדר וצפוי) — לכן **חובה RLS (Row Level Security) על כל טבלה** מהיום הראשון. משתמש רואה רק את השורות שלו.
- **מגבלות החינמי שחייבים להכיר**:
  - 50,000 משתמשים פעילים/חודש, 500MB DB, 1GB storage — מספיק בשפע לשנים הראשונות.
  - ⚠️ **פרויקט חינמי נכנס להשהיה אחרי ~שבוע ללא פעילות.** פתרון: GitHub Action שמבצע פינג שבועי ל־DB (חינם). כשיש הכנסות — Pro ($25/חודש) מבטל את זה ומוסיף גיבויים יומיים.
- כדאי לרכוש **דומיין** (~50₪/שנה) כבר עכשיו — חברות מזון ומאמנים לא ייקחו ברצינות כתובת github.io, וזה גם נדרש ל־redirect של Auth.

## שלב 2 — גרסת מאמנים (White-label) — מנוע הצמיחה

לפני חברות המזון, כי זה מה שמביא את המשתמשים שחברות המזון משלמות עליהם.

- **מודל**: מאמן נרשם → מעלה לוגו + בוחר צבע → מקבל **קישור אישי** `dietai.co.il/?coach=danny` (ובהמשך תת-דומיין). מתאמן שנכנס מהקישור רואה את האפליקציה עם המיתוג של המאמן — "הדמות שמאחורי התוכנה" היא המאמן.
- **טכני**: טבלת `coaches` (שם, לוגו ב־Supabase Storage, צבע ראשי, סלוגן) + פרמטר URL שמחליף לוגו/צבעים ב־CSS variables. המתאמנים משויכים למאמן (`profiles.coach_id`) — המאמן רואה כמה מתאמנים פעילים יש לו.
- **מודל עסקי**: ראו סעיף "המודל העסקי" למעלה — חינם בהתחלה, מנוי פרו כשיש ערך מוכח. נבנה רק אם שלב 0.5 הראה ביקוש.
- מה שלא סגור עדיין (החלטות עתידיות): איך נראה דף ניהול המאמן, האם המאמן רואה את נתוני המתאמנים (דורש הסכמה — פרטיות).

## שלב 3 — Retention: שהמתאמנים יחזרו כל שבוע

מאמן ישלם רק אם המתאמנים שלו משתמשים באפליקציה באמת. לכן תועלת שימושית קודמת לגיימיפיקציה:
- **שמירת תפריטים והיסטוריה** ("התפריט שעבד לי") — כבר יש טבלת menus משלב 1.
- **רשימת קניות מהתפריט** — נגזרת אוטומטית מהפריטים; ערך יומיומי אמיתי.
- **ייצוא PDF / שיתוף** — המתאמן שולח למשפחה, המאמן מדפיס.
- תפריט שבועי (7 ימים מגוונים) — שדרוג טבעי של "תפריט נוסף".

## שלב 3.5 — נקודות ומעורבות (אחרי שיש Retention)

- `points` בטבלת profiles. צבירה: כניסה יומית, יצירת תפריט, שיתוף, הזמנת חבר.
- מימוש: פתיחת הפינוקים משלב 0, ערכות נושא.
- לשמור על פשטות — לא קזינו. הנקודות הן תירוץ לחזור לאפליקציה, לא כלכלה שלמה.

## שלב 4 — חברות מזון (המונטיזציה המרכזית)

נכנסים לזה רק כשיש מספרים להראות (מהאנליטיקס של שלב 1).

- **טכני**: טבלת `sponsored_products` — מוצר ממותג (למשל "קוטג' תנובה 3%") עם הערכים התזונתיים שלו, תמונה שהחברה מספקת, ותקופת קמפיין. האלגוריתם נשאר זהה — כשנבחר "קוטג'", המוצר הממומן מקבל עדיפות הצגה עם התמונה הממותגת.
- **קו אדום מקצועי**: מוצר ממומן חייב להתאים תזונתית לתפריט (אותם ערכים/קטגוריה). לא דוחפים חטיף לתפריט חיטוב כי שילמו — זה הורס את האמון שהוא כל הנכס.
- ⚖️ **חובת סימון**: תוכן ממומן חייב להיות מסומן ("בחסות X" / "ממומן") — דרישת חוק הגנת הצרכן וגם הדבר הנכון. שקיפות = אמון.
- **תמחור** (כיוון ראשוני): חבילת קמפיין חודשית לפי חשיפות (כמה תפריטים כללו את המוצר — יש לנו את זה ב־events).

---

## סיכום סדר ביצוע

| # | מה | תלוי ב | מאמץ | מתי |
|---|----|--------|------|-----|
| 0 | תפריט עם פינוק (קפה/שתיה) | — | קטן | עכשיו |
| 0.5 | דף נחיתה למאמנים + טופס (ולידציה) | — | קטן מאוד | עכשיו/במקביל |
| 1 | Supabase: Auth + שמירת פרופיל/תפריטים + events + RLS + דומיין | — | בינוני | הבא |
| 2 | גרסת מאמנים ממותגת | 1 + ביקוש מ‑0.5 | בינוני | אחרי 1 |
| — | ייעוץ משפטי (עיסוק מוסדר) | לפני גבייה | קטן | לפני מנוי מאמנים |
| 3 | Retention: היסטוריה, רשימת קניות, PDF, תפריט שבועי | 1 | בינוני | אחרי 2 |
| 3.5 | נקודות + נעילת פינוקים | 3 | קטן-בינוני | אחרי 3 |
| 4 | מוצרים ממומנים | 1+2 (מספרים) | בינוני | כשיש משתמשים |

**עלות חודשית עד לצמיחה: 0₪** (Supabase free + GitHub Pages) למעט דומיין (~50₪/שנה). נקודת השדרוג הראשונה: Supabase Pro ($25/חודש) כשיש תנועה אמיתית — בשביל גיבויים וביטול ההשהיה.
